# Smart Campus Planner — 단계별 구현 요청 프롬프트 (PROMPTS)

이 문서는 `PRD.md`(프로젝트 목적/요구사항/MVP)와 `TASKS.md`(3단계 작업 계획)를 바탕으로,
AI 코딩 에이전트(Cline 등)에게 **한 단계씩 순서대로 구현을 요청**할 때 그대로 복사해서 사용할 수 있는
프롬프트 모음입니다.

## 사용 방법 (비전공자를 위한 안내)
1. 아래 "1단계 프롬프트"를 그대로 복사해서 AI에게 전달합니다.
2. AI가 구현을 마치면, 직접 화면을 열어보고 "이 단계가 끝나면 확인할 수 있는 것"에 적힌 대로 잘 동작하는지 확인합니다.
3. 문제가 없으면 "2단계 프롬프트"로 넘어갑니다. 문제가 있으면 무엇이 이상한지 AI에게 설명하고 먼저 고친 뒤 다음 단계로 넘어갑니다.
4. 같은 방식으로 3단계까지 순서대로 진행합니다. **단계를 건너뛰거나 한 번에 여러 단계를 동시에 요청하지 않는 것이 중요합니다.**

---

## 1단계 프롬프트 — 할 일 입력 & 목록 화면 만들기

```
현재 프로젝트 폴더에 있는 PRD.md와 TASKS.md 파일을 참고해서,
TASKS.md의 "1단계. 할 일 입력 & 목록 화면 만들기"에 해당하는 기능만 구현해줘.

이번 단계에서 만들어야 하는 것:
- 할 일 제목을 입력하는 칸
- 카테고리를 선택하는 항목 (수업 / 프로젝트 / 논문 / 개인 중 하나 선택)

- 중요도를 선택하는 항목 (상 / 중 / 하 중 하나 선택)
- 마감일을 선택하는 날짜 입력 칸
- "추가" 버튼을 누르면 입력한 내용이 화면 아래 목록에 새로운 항목으로 나타나야 함
- 각 목록 항목에는 제목, 카테고리, 중요도, 마감일이 보기 좋게 표시되어야 함

아직 구현하지 않아도 되는 것 (다음 단계에서 진행할 예정이니 이번에는 만들지 마):
- 완료 체크, 삭제 기능
- 전체 완료율 표시
- 새로고침해도 데이터가 유지되는 저장 기능

특별한 프레임워크 설치 없이 웹 브라우저에서 바로 열어서 확인할 수 있는 간단한 웹페이지 형태로 만들어줘.
구현이 끝나면 어떤 파일을 만들었는지, 그리고 브라우저에서 어떻게 열어서 확인하면 되는지 알려줘.
```

### 확인 포인트 (구현 후 직접 눈으로 확인)
- 제목, 카테고리, 중요도, 마감일을 입력하고 "추가" 버튼을 누르면 목록에 새 항목이 바로 보이는지
- 여러 개를 계속 추가했을 때 목록에 모두 쌓여서 보이는지
- 카테고리와 중요도가 각각 정해진 3가지 선택지 중에서만 고를 수 있는지
- (새로고침하면 목록이 사라져도 이번 단계에서는 정상입니다.)

---

## 2단계 프롬프트 — 할 일 관리 기능 붙이기 (완료 체크 / 삭제 / 완료율)

```
현재 프로젝트 폴더에 있는 PRD.md와 TASKS.md 파일을 참고하고,
지금까지 구현되어 있는 화면(1단계 결과물)을 그대로 이어서,
TASKS.md의 "2단계. 할 일 관리 기능 붙이기"에 해당하는 기능만 추가로 구현해줘.

이번 단계에서 추가해야 하는 것:
- 목록의 각 할 일 항목 옆에 완료 체크박스(또는 버튼)를 추가해서, 누르면 완료 상태로 표시되고
  다시 누르면 미완료 상태로 되돌아가야 함 (완료된 항목은 취소선이나 흐린 색 등으로 구분되면 좋음)
- 목록의 각 할 일 항목 옆에 삭제 버튼을 추가해서, 누르면 그 항목이 목록에서 즉시 사라져야 함
- 화면에 "전체 할 일 중 몇 %를 완료했는지" 보여주는 완료율 표시를 추가하고,
  할 일을 추가/완료 체크/삭제할 때마다 이 완료율이 자동으로 다시 계산되어야 함
- 할 일이 하나도 없을 때 완료율은 오류 없이 0%(또는 적절한 기본값)로 표시되어야 함

1단계에서 만든 입력 기능(제목, 카테고리, 중요도, 마감일 추가)은 그대로 유지하고 건드리지 마.
아직 구현하지 않아도 되는 것: 새로고침해도 데이터가 유지되는 저장 기능은 다음 단계에서 진행할 예정이니 이번에는 만들지 마.

구현이 끝나면 무엇이 바뀌었는지, 어떻게 테스트해보면 되는지 알려줘.
```

### 확인 포인트 (구현 후 직접 눈으로 확인)
- 할 일을 완료 체크하면 완료 표시가 나타나고, 완료율 숫자가 즉시 올라가는지
- 완료 체크를 취소하면 완료율이 다시 내려가는지
- 삭제 버튼을 누르면 해당 항목이 목록에서 사라지고, 완료율도 그에 맞게 다시 계산되는지
- 할 일이 하나도 없을 때, 전부 완료했을 때 등 여러 상황에서 완료율이 이상한 값(예: 오류, NaN) 없이 올바르게 표시되는지

---

## 3단계 프롬프트 — 새로고침해도 유지되도록 저장하기

```
현재 프로젝트 폴더에 있는 PRD.md와 TASKS.md 파일을 참고하고,
지금까지 구현되어 있는 화면(1~2단계 결과물)을 그대로 이어서,
TASKS.md의 "3단계. 새로고침해도 유지되도록 저장하기"에 해당하는 기능만 추가로 구현해줘.

이번 단계에서 해야 하는 것:
- 할 일을 추가하거나, 완료 체크를 하거나, 삭제할 때마다 그 변경된 내용이 브라우저에 자동으로 저장되어야 함
- 웹페이지를 새로고침하거나 다시 열었을 때, 저장되어 있던 할 일 목록(완료 상태 포함)을 그대로 불러와서 화면에 보여줘야 함
- 완료율 표시도 새로고침 후 불러온 데이터를 기준으로 정확하게 다시 계산되어야 함
- 처음 사용하는 경우(저장된 데이터가 없는 경우)에는 오류 없이 빈 목록으로 정상적으로 시작되어야 함

1~2단계에서 만든 기능(추가, 완료 체크, 삭제, 완료율 표시)은 그대로 유지하고 동작 방식은 바꾸지 마.
이 단계를 완료하면 PRD.md에 정의된 MVP 범위(FR-1~FR-8)가 모두 구현되는 것이니,
구현이 끝나면 MVP 요구사항이 모두 충족되었는지 간단히 정리해서 알려줘.
그리고 새로고침 후 데이터가 유지되는지 어떻게 테스트해보면 되는지도 알려줘.
```

### 확인 포인트 (구현 후 직접 눈으로 확인)
- 할 일을 몇 개 추가하고 일부는 완료 체크한 뒤 브라우저를 새로고침해도 목록, 완료 상태, 완료율이 그대로 남아있는지
- 브라우저 탭이나 창을 완전히 닫았다가 다시 열어도 데이터가 유지되는지
- 새로 추가/완료/삭제한 내용이 계속 정상적으로 반영·저장되는지
- 이 단계까지 확인되면 PRD.md의 MVP(FR-1~FR-8)가 모두 완성된 것입니다.

---

## 4단계 프롬프트 — 디자인 개선 & 세부 기능 다듬기 (MVP 이후 추가 개선)

```
현재 프로젝트 폴더에 있는 index.html, style.css, script.js 파일을 그대로 이어서,
아래 내용을 반영해서 디자인과 세부 기능을 다듬어줘.

디자인 관련 요청:
- 라벤더 · 옐로우 · 민트 · 블루 · 핑크 파스텔 팔레트를 사용한 "파스텔 모던 To-Do" 톤앤매너로 전체 디자인을 변경
- 카테고리(수업/프로젝트/논문/개인) 태그는 각각 서로 다른 색상 계열로 구분하고, 같은 카테고리 안에서는 배경/텍스트 색을 통일
- 중요도(상/중/하) 태그는 하나의 색상 계열(예: 붉은/분홍 계열) 안에서 진하기로만 구분하고, 텍스트 색도 계열별로 통일
- 상단 문구, 하단 문구는 딱딱한 영어 문구 대신 자연스럽고 아기자기한 한국어 문구 + 이모지로 표현

기능 관련 요청:
- 카테고리 선택지를 학과 / 동아리 / 개인 3개에서 수업 / 프로젝트 / 논문 / 개인 4개로 변경
- 마감일(dueDate) 입력 시 오늘보다 이전 날짜는 선택할 수 없도록 제한 (날짜 입력창의 min 속성 + 폼 제출 시 검증)

기존에 구현되어 있는 추가/완료 체크/삭제/완료율/정렬/저장(localStorage) 기능의 동작 방식은 그대로 유지하고 건드리지 마.
```

### 확인 포인트 (구현 후 직접 눈으로 확인)
- 카테고리 선택지가 수업 / 프로젝트 / 논문 / 개인 4개로 바뀌어 있는지
- 카테고리별 태그 색상이 서로 다른 계열로 구분되어 보이는지
- 마감일 입력 시 오늘 이전 날짜를 고를 수 없는지 (달력에서 비활성화되어 있는지)
- 기존 추가/완료/삭제/완료율/저장 기능이 이전과 동일하게 잘 동작하는지

---

## 참고 사항

- 각 프롬프트는 이전 단계가 정상적으로 동작하는 것을 직접 확인한 뒤에 사용하는 것을 권장합니다.
- 프롬프트를 그대로 사용해도 되고, 필요하다면 화면 디자인이나 문구 등 세부 표현은 자유롭게 바꿔서 사용해도 됩니다.
- 본 문서는 구현 요청에 사용할 "프롬프트"만을 정리한 것으로, 실제 프로그램 코드는 포함되어 있지 않습니다.
