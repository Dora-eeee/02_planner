# Smart Campus Planner — 단계별 작업 계획 (TASKS)

이 문서는 `PRD.md`에 정리된 요구사항(FR-1~FR-8)과 MVP 범위를 한 번에 구현하지 않고,
3단계로 나누어 순서대로 진행하기 위한 계획입니다.
각 단계는 이전 단계의 결과물 위에서 다음 기능을 이어 붙이는 방식으로 설계되어 있습니다.

---

## 전체 진행 순서 요약

| 단계 | 이름 | 관련 요구사항 | 한 줄 요약 |
|------|------|----------------|------------|
| 1단계 | 할 일 입력 & 목록 화면 만들기 | FR-1, FR-2, FR-3, FR-4 | 할 일을 새로 등록하고 목록으로 볼 수 있는 기본 화면 만들기 |
| 2단계 | 할 일 관리 기능 붙이기 | FR-5, FR-6, FR-7 | 등록된 할 일을 완료 처리하거나 지우고, 전체 완료율을 보여주기 |
| 3단계 | 새로고침해도 유지되도록 저장하기 | FR-8 | 브라우저를 껐다 켜거나 새로고침해도 데이터가 사라지지 않게 만들기 |

---

## 1단계. 할 일 입력 & 목록 화면 만들기

**관련 요구사항:** FR-1(할 일 추가), FR-2(카테고리 설정), FR-3(중요도 설정), FR-4(마감일 지정)

### 쉬운 설명
가장 먼저, 사용자가 할 일을 "적어 넣을 수 있는 입력창"과 그렇게 적은 할 일들이 "목록으로 보이는 화면"을 만드는 단계입니다.
아직 완료 체크나 삭제, 저장 같은 기능은 없고, 오직 "할 일을 새로 만들고 화면에 나열해서 보여주는 것"까지만 구현합니다.

이 단계에서 만들어지는 화면 예시:
- 할 일 제목을 입력하는 칸
- 카테고리를 고르는 선택지 (학과 / 동아리 / 개인)
- 중요도를 고르는 선택지 (상 / 중 / 하)
- 마감일을 고르는 날짜 선택 칸
- "추가" 버튼을 누르면 위에서 입력한 정보가 아래 목록에 카드나 줄 형태로 나타남

### 이 단계가 끝나면 확인할 수 있는 것
- 할 일 제목, 카테고리, 중요도, 마감일을 입력하고 등록 버튼을 누르면, 목록에 새 항목이 바로 추가되어 보인다.
- 여러 개의 할 일을 계속 등록해서 목록에 쌓을 수 있다.
- (아직 새로고침하면 목록이 사라져도 괜찮은 단계입니다. 저장 기능은 3단계에서 추가합니다.)

### 다음 단계와의 연결
2단계에서는 이 목록에 있는 "각각의 할 일 항목"에 완료 체크박스와 삭제 버튼을 붙이게 되므로,
1단계에서 목록 항목이 화면에 잘 나열되는 구조가 먼저 갖춰져야 합니다.

---

## 2단계. 할 일 관리 기능 붙이기

**관련 요구사항:** FR-5(완료 체크), FR-6(삭제), FR-7(전체 완료율 표시)

### 쉬운 설명
1단계에서 만든 목록에, 각 할 일을 "끝냈어요" 표시하거나 "필요 없어졌어요"라며 지울 수 있는 기능을 추가하는 단계입니다.
또한 전체 할 일 중 몇 퍼센트를 끝냈는지 한눈에 보여주는 진행률 표시도 이 단계에서 만듭니다.

이 단계에서 추가되는 것:
- 목록의 각 할 일 옆에 체크박스(또는 완료 버튼)를 넣어서, 누르면 완료 상태로 바뀌고 다시 누르면 미완료로 돌아가게 함
- 목록의 각 할 일 옆에 삭제 버튼을 넣어서, 누르면 그 할 일이 목록에서 사라지게 함
- 화면 상단이나 눈에 잘 띄는 곳에 "전체 할 일 중 몇 %를 완료했는지" 보여주는 표시(숫자, 퍼센트, 또는 막대바 형태)를 추가하고, 완료 체크를 하거나 삭제할 때마다 이 숫자가 자동으로 다시 계산되어 바뀌게 함

### 이 단계가 끝나면 확인할 수 있는 것
- 할 일을 완료로 체크하면 완료 표시(예: 취소선, 색 변화 등)가 나타나고, 완료율 숫자가 올라간다.
- 할 일을 삭제하면 목록에서 즉시 사라지고, 완료율 숫자도 그에 맞게 다시 계산된다.
- 할 일을 하나도 추가하지 않았을 때, 여러 개를 추가했을 때, 전부 완료했을 때 등 다양한 상황에서 완료율이 올바르게 표시된다.

### 다음 단계와의 연결
지금까지는 할 일 데이터가 화면(메모리)에만 잠깐 머물러 있어서 새로고침하면 사라집니다.
3단계에서는 이 데이터를 "저장"해서 새로고침해도 그대로 남아있도록 만들 것이므로,
2단계까지 완성된 "추가-완료-삭제-완료율" 흐름이 먼저 정확하게 동작해야 합니다.

---

## 3단계. 새로고침해도 유지되도록 저장하기

**관련 요구사항:** FR-8(데이터 유지/저장)

### 쉬운 설명
지금까지 만든 기능(추가, 완료 체크, 삭제, 완료율 표시)은 잘 작동하지만, 브라우저를 새로고침하거나 창을 닫았다가 다시 열면
입력했던 할 일들이 모두 사라지는 문제가 있습니다. 이 단계에서는 그 문제를 해결해서, 사용자가 등록한 할 일이
새로고침 후에도, 브라우저를 다시 켜도 그대로 남아있도록 만듭니다.

이 단계에서 하는 일:
- 할 일을 추가/완료 체크/삭제할 때마다 그 변경 내용을 브라우저에 자동으로 저장
- 웹페이지를 처음 열거나 새로고침할 때, 저장되어 있던 할 일 데이터를 불러와서 화면에 그대로 다시 보여줌
- 저장된 할 일이 하나도 없을 때(처음 사용하는 경우)는 빈 목록으로 정상적으로 시작되도록 확인

### 이 단계가 끝나면 확인할 수 있는 것
- 할 일을 몇 개 추가하고 완료 체크도 한 뒤 새로고침을 해도, 추가했던 할 일과 완료 상태, 완료율이 그대로 남아있다.
- 브라우저 탭이나 창을 완전히 닫았다가 다시 열어도 이전에 입력한 내용이 그대로 유지된다.
- 이 단계까지 완료되면 PRD에 정의된 MVP 범위(FR-1~FR-8)가 모두 충족된다.

### 다음 단계와의 연결
3단계까지 완료되면 PRD의 MVP 요구사항이 모두 구현되어, Smart Campus Planner의 첫 번째 완성 버전(MVP)이 완성됩니다.
이후 추가 개선(다중 기기 동기화, 알림, 캘린더 뷰 등)은 PRD의 "범위 외 사항"에 따라 별도로 논의합니다.

---

## 참고 사항
- 본 문서는 작업 "계획"만을 정리한 것으로, 실제 프로그램 코드는 이 문서에 포함되어 있지 않습니다.
- 각 단계는 이전 단계가 정상적으로 동작하는 것을 확인한 뒤 다음 단계로 넘어가는 것을 권장합니다.
